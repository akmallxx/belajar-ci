<?= $this->extend('pegawai/layout.php'); ?>
<?= $this->section('content'); ?>

<a href="<?= base_url('ketidakhadiran/create') ?>" class="btn btn-primary"><i class="bi bi-plus-circle"></i>Tambah Data</a>

<table class="table table-striped table-bordered" id="datatables">
    <thead class="thead-dark">
        <tr>
            <th scope="col">No</th>
            <th scope="col">Deskripsi</th>
            <th scope="col">Keterangan</th>
            <th scope="col">Tanggal</th>
            <th scope="col">File</th>
            <th scope="col">Detail</th>
            <th scope="col">Aksi</th>
        </tr>
    </thead>
    <tbody>
        <?php
        $no = 1;
        foreach ($ketidakhadiran as $kh): ?>
            <tr>
                <td><?= $no++ ?></td>
                <td><?= htmlspecialchars($kh['deskripsi']) ?></td>
                <td><?= htmlspecialchars($kh['keterangan']) ?></td>
                <td><?= htmlspecialchars(date('d-m-Y', strtotime($kh['tanggal_awal'])) . ' - ' . date('d-m-Y', strtotime($kh['tanggal_akhir']))) ?></td>
                <td><a href="<?= base_url('uploads/' . $kh['file']) ?>" target="_blank"><?= htmlspecialchars($kh['file']) ?></a></td>
                <td>
                    <a href="<?= base_url('ketidakhadiran/detail/' . $kh['id']) ?>" class="badge bg-secondary">Klik untuk detail</a>
                </td>
                <td>
                    <a href="<?= base_url('ketidakhadiran/edit/' . $kh['id']) ?>" class="badge bg-primary">Edit</a>
                    <a href="<?= base_url('ketidakhadiran/delete/' . $kh['id']) ?>" class="badge bg-danger delete-button">Hapus</a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<?= $this->endSection(); ?>
